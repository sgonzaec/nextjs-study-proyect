import { Metadata } from "next"

export const metadata: Metadata = {
    title: 'Pricing Page',
    description: "Esta es la pagina sobre precios",
    category: 'Prices'
}

export default function PricingPage() {
    return (
        <span>Pricing</span>
    )
}